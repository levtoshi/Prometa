from imports import *
from win import win
def snake_start():
    print('In process')

'''    win.geometry('800x850+550+150')
    win.title('Snake')

    l_fg_color = 'black'
    l_g = 99

    apple_ico = CTkImage(dark_image=Image.open('button_icons/apple.png'),
    light_image=Image.open('button_icons/apple.png'), size=(50, 50))

    frame = CTkFrame(win)



    labels = []

    for i in range(0,100):
        labels.append(str(i))
        labels[i] = CTkLabel(win, text='', fg_color=l_fg_color, width=l_g, height=l_g)
        labels[i].place(x=(i * 80) // 800, y=(i * 80) // 800)


    print(labels)

    class snake:

        def __init__(self, len, lab_place):
            self.len = len
            self.lab_place = lab_place

        def start(self):
            labels[1]
'''


